import { IClassNames } from './IClassNames';
import { IStyleFunction } from './IStyleFunction';
/**
 * Creates a getClassNames function which calls getStyles given the props, and injects them
 * into mergeStyleSets.
 */
export declare function classNamesFunction<TStyleProps extends {}, TStyles extends {}>(): (getStyles?: IStyleFunction<TStyleProps, TStyles>, styleProps?: TStyleProps) => IClassNames<TStyles>;
